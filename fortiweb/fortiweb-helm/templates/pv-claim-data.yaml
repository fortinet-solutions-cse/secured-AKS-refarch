apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fortiweb-persistent-storage-data
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 2Gi